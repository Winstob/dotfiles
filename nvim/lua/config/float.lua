local M = {}

M.opts = {
    border = "rounded",      -- adds a border
    source = "always",       -- show which LSP produced it
    header = "",             -- remove noisy header
    prefix = "",             -- remove extra prefix
    max_width = 80,
}

return M
